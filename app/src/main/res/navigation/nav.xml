<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/menu">

    <fragment
        android:id="@+id/menu"
        android:name="com.pbmario.pruebagraficas.menu"
        android:label="fragment_menu"
        tools:layout="@layout/fragment_menu" />
    <fragment
        android:id="@+id/barras"
        android:name="com.pbmario.pruebagraficas.barras"
        android:label="fragment_barras"
        tools:layout="@layout/fragment_barras" >
        <action
            android:id="@+id/action_barras_to_menu"
            app:destination="@id/menu" />
    </fragment>
</navigation>